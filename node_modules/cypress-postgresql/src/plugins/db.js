module.exports = ( pool ) => {
    return {
        'postgreSQL:query': ( sql ) => {
            try {
                return pool.query(sql);
            } catch (err) {
              console.log(err.stack);
            }
        }
    };
};